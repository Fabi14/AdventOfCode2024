📋 ← &fras "input.txt"
Parse ← ≡⍜(°□) (⊜⋕≠@ .) ⊜□≠@\n.

SameSign ← ≠0/+⊂∩(/×)⊃(=¯1)(=1)±
OneAndThree ← /×⊂⊃(≤3)(≥1)⌵
TestLevel ← /×⊂⊃OneAndThree SameSign ≡/-◫2

Variants ← ⊂⊙□≡(□▽≠0.) ×⊙(↯⧻)⊞≠.⇡⧻...

① ← /+≡(TestLevel°□)
② ← /+≡(≠0/+≡(TestLevel°□)Variants°□) # TODO part 2

① Parse 📋
② Parse 📋
